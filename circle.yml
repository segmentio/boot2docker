machine:
  pre:
    - curl -LO https://dl.bintray.com/mitchellh/packer/packer_0.7.5_linux_amd64.zip
    - unzip -o packer_0.7.5_linux_amd64.zip -d /home/ubuntu/bin/
dependencies:
  override:
    - packer version
test:
  override:
    - packer validate template.json
