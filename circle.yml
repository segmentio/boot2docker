machine:
  pre:
    - curl -LO https://dl.bintray.com/mitchellh/packer/packer_0.7.5_linux_amd64.zip
    - unzip packer_0.7.5_linux_amd64.zip
dependencies:
  override:
    - ls -l
test:
  override:
    - echo "ok"
